import type { ComponentPropsWithRef } from "react";

export type ConnectionProps = ComponentPropsWithRef<"svg"> & { fromX: number; fromY: number; toX: number; toY: number; color?: string; animated?: boolean };
export function Connection({ fromX, fromY, toX, toY, color = "currentColor", animated = true, className = "", ...props }: ConnectionProps) { const offset = Math.max(24, Math.abs(toX - fromX) * 0.45); const path = `M ${fromX} ${fromY} C ${fromX + offset} ${fromY}, ${toX - offset} ${toY}, ${toX} ${toY}`; return <svg {...props} className={`hk-connection ${className}`} role={props.role ?? (props["aria-label"] ? "img" : undefined)} aria-hidden={props["aria-hidden"] ?? (props["aria-label"] ? undefined : true)} focusable="false"><path d={path} fill="none" stroke={color} className={animated ? "hk-connection-line hk-connection-line--animated" : "hk-connection-line"} /><circle cx={toX} cy={toY} r="4" fill={color} /></svg>; }
