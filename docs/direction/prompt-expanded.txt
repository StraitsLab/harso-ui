Two images. IMAGE 1 is the APPROVED design direction for Weave — copy its taste exactly: pure white canvas, no borders, no dividers, no boxes-in-boxes; separation by whitespace only; a 64px icon-only left rail of fine-line glyphs with no highlight pills; a right context panel whose sections are separated by 40px of air and titled in small muted grey; indicators tiny (5px amber dot, 14px blue four-point star; the only ring is a 28px thin progress ring); one editorial headline in the assistant's reply, 14–15px body with generous line-height; a floating pill prompt bar at the bottom with a solid dark circular send button; monoline 1.5px icons; calm, intelligent, refined.

IMAGE 2 is the CURRENT build of the conversation screen. Its CAPABILITY is the requirement — every element in it must survive, nothing may be dropped or merged away: window traffic lights; sidebar with New conversation, Search, Activity (unread count), Artifacts, Routines, Customize, All projects › Personal / Weave Cloud, three Recent conversations, the account row; toolbar with ‹ › navigation, the conversation title and 'Personal · 14 turns', ⋯ menu, share, search, inspector toggle; the thread: user message with timestamp, 'Reasoned for 18 seconds' disclosure, the assistant's answer with its three numbered steps and the TypeScript excerpt with file path, the 'npm test' run status with 'Permission needed', copy / thumbs / regenerate actions; the composer with +, model selector 'Claude Fable 5.1', microphone, send; the right column with Context (project, path, instructions), Changed files (three paths with +/− counts), Approvals ('Review npm test', waiting 2 minutes), context window usage 41.2k of 200k, session metadata. All of it, same words.

Task: redraw IMAGE 2 so it looks like IMAGE 1 designed it. Same elements, radically less visual weight: the sidebar becomes a quiet 240px source list with fine-line glyphs and text, no pills except a faint tint on the active row; no column dividers — the sidebar and inspector are a barely-warmer white; the toolbar has no rule under it; the code excerpt and the composer are the only two shaded surfaces (soft 12–16px, ambient shadow, no border); the run status and the approval become single quiet rows with a tiny amber dot, not cards; the context-usage ring shrinks to a 16px hairline ring beside its text; secondary actions are 1.5px monoline icons with no boxes; 'Reasoned for 18 seconds' is small grey text with a chevron; one blue, one amber, otherwise neutral; SF Pro, one type scale (11 caps labels, 13 chrome/meta, 14 body, 15 title), 8px rhythm, generous air between blocks. Native macOS 27 window, 1440×900. Photorealistic UI render, crisp text, no artefacts.
