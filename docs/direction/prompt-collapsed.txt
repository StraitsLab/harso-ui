Two images. IMAGE 1 is the APPROVED design direction for Weave: a 64px icon-only left rail of fine-line 1.5px glyphs with generous spacing and no highlight pills, pure white borderless canvas, sections separated by air, tiny indicators, one blue + one amber accent, floating pill prompt bar, calm and refined. IMAGE 2 is the current conversation screen with its sidebar expanded.

Draw IMAGE 2 with the sidebar COLLAPSED into the rail from IMAGE 1: a 64px rail holding, top to bottom, the traffic lights, then fine-line glyphs for New conversation (square-and-pencil), Search, Activity (bell with a tiny 5px blue dot for unread), Artifacts, Routines, Customize, a folder for Projects, and at the very bottom the account avatar (28px circle 'AB') — no labels, no pills, the active glyph simply darker. Everything else on the screen keeps every element and every word from IMAGE 2 (toolbar with ‹ ›, title, meta, ⋯, share, search, inspector toggle; the full thread; composer with +, model selector, mic, send; the right column with Context, Changed files, Approvals, context usage, session) but rendered with IMAGE 1's restraint: no borders or dividers, the code excerpt and the composer the only two shaded surfaces, run status and approval as quiet single rows with a tiny amber dot, a 16px hairline context-usage ring, monoline icons with no boxes, SF Pro on one type scale (11/13/14/15), 8px rhythm, plenty of air. Native macOS 27 window, 1440×900. Photorealistic UI render, crisp text, no artefacts.
