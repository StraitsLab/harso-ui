# Complete library execution ledger

Latest reconciled accounting is maintained in COVERAGE.md and coverage.json
under WEV-1498; all 126 families and 2,841 requirement identities are preserved.
This pass reconciles independently reviewed lifecycle/refusal and visual proofs,
repairs short-window menu clearance, and corrects an offscreen tooltip fixture.
Native gallery dismissal replaces the event-rewriting workaround. The current
bounded presentation review and concrete repairs are closed as construction
evidence; real-device/AT and speech-output obligations remain unverified.
See VERIFICATION.md for execution identities and the full REMAINING.md list.
The library is not fully ready. Historical checkpoints below are superseded only
by the exact evidence changes retained in coverage.json.

## WEV-1492 — current verification candidate

This phase preserves the original Boundaryless design and the immutable reference
inventory. It reconciles prior evidence and adds focused proof, not another GUI
redesign. A reproduced accessible-name defect is repaired in the marketing and
medical gallery wrappers and their shared layout selectors. Backend, runtime,
dependencies, branches, commits, merges and releases remain untouched. Desktop
adoption remains separate.

The complete current accounting is in `COVERAGE.md` and `coverage.json`; every
open exact row is in `REMAINING.md`. Evidence history is preserved per row rather
than replacing the prior checkpoint. Native audio, foundation contracts, prior
questionnaire/question behavior, AI/developer compositions and chart/template
observables receive new runnable tests. Independent review rejects superficial
pagination/count checks and requires observed row replacement and actual chart
series/metric values before those claims close.

Execution artifacts and worker reports are under
`/tmp/harso-verification-1492`; `VERIFICATION.md` records final results and source
identities. Original-equivalent export availability is an export-only claim, not
whole-family proof. Combined assistive-technology obligations and unreviewed
visual combinations remain open. This is not Land acceptance or full readiness.

## WEV-1489 — parallel reconciliation candidate

This is the preceding batch; entries below retain their historical status
and evidence. The complete 126-family table is in `COVERAGE.md`, every immutable
requirement row is in `coverage.json`, and `REMAINING.md` enumerates open rows.
Do not equate exports, a green default-render matrix, or zero rows classified
missing with full-library acceptance. Unverified requirements remain open.

The batch uses original Boundaryless implementations against the saved public
BoardUI/AI Elements checklist, without Pro or gated code. Six disjoint worker
slices cover foundation, AI/work, developer content, media, charts and templates;
the lead integrates the shared gallery, regressions and documentation.
The requested Astra Light model identifier was not available; inherited-model
workers were used, not a verified Astra Light mapping.

Repairs include native input/focus/reset behavior, host-controlled composer
composition and progress, chart-specific geometry and calendar data, notification
presence/timers, preview sandbox capability stripping and credential-free image
sources. Public functional equivalents are documented rather than disguised as
vendor-compatible APIs. No runtime, auth, upload or persistence authority moves
into the UI kit.

Evidence directory: `/tmp/harso-parallel-library-0908`. Source/test identities
are captured in `final-source-before.json`; final verification records whether
those identities stayed stable. Independent security and foundation challenges
record both the original counterexamples and their scoped rechecks. These are
candidate checks, not Land acceptance or whole-product validation.

The final stabilized run uses `final-candidate-before.json` after subsequent
presence and gallery-fixture repairs. Closing notifications/loaders now applies
`aria-hidden` and `inert` during the visible CSS exit, not only after display:none.
The independent affected-claim recheck is
`security-review/presence-0205/recheck.md`: new focus and keyboard activation are
blocked during exit, while an already-focused descendant can remain active until
the browser's next frames. No synchronous focus-restoration claim is made.

The broad browser pass also exposed outdated fixture assumptions: native modal
backgrounds cannot be clicked, attachment inputs may be disabled/internal, and
native theme transitions settle asynchronously. Modal examples now expose their
host-refusal test controls inside the modal; upload tests use the actual file
chooser; settled contrast audits still run Axe without disabling rules. Eight
older specs now use the configured preview origin rather than a fixed4192 URL.
Their external-request assertions remain strict actual-origin/protocol checks.

Independent coverage review downgraded29 combined keyboard/focus/assistive-tech
obligations because no actual screen-reader session was recorded. Typography's
stateless host-refusal row is inapplicable; Commit's combined state row remains
unverified until all applicable state evidence is bound. The exact status totals
in `COVERAGE.md` include these corrections, not just worker-reported passes.

Current preview custody is the dedicated local library server on4194, started
by this batch. Existing4186/4191/4192/4193 servers are not taken over. The disposable
build is outside the repository. No branch, commit, merge, release or backend
change is part of this batch; desktop adoption remains a separate scope.

## WEV-1478 — Questionnaire in progress

Original stepped Questionnaire implementation added from the saved public reference,
with native radio/checkbox rows, ordered keyed answers, Other text, single-choice
delay, controlled/default state, step navigation, labels and dismissal. Exports and
scoped CSS are wired. No Pro source or dependencies were added.

Initial test run demonstrated the missing module (collection failure, not behavioral
RED). Nine focused tests now pass, as do UI typecheck and the full 388-test suite.
Fake-timer assertions check mounted step content rather than animation visibility;
actual animation visibility remains a browser proof obligation. Evidence logs:
`/tmp/harso-questionnaire-{red,test,typecheck,full}.log`.

Catalogue mapping now includes the separately hashed September 7 public capture:
126 families, 422 constituent part references and 21 helper APIs. Questionnaire has
one visual export, no invented public part exports, and QuestionnaireAnswers as its
documented helper. Original September 6 discovery identities remain unchanged.
The interactive preview covers mixed/multiple/single modes and empty input.

Current verification: 392 UI tests, UI and preview typechecks, isolated preview build,
and 10 actual Chrome checks pass. Browser checks cover answer retention, single-choice
submission, Other Enter, local digit selection, dismissal/reopening, focus transfer,
1512/390 widths and Light/Dark × Clean/Cozy with reduced motion and Axe. Wide Light
Clean and narrow Dark Cozy screenshots inspected. Evidence:
`/tmp/harso-questionnaire-1478/`. Existing large-bundle warnings remain.

Independent review found two real defects: restoring a superseded controlled answer
revived its old advance intent, and clicking an already-selected radio after returning
did not advance. Both reproduced locally (2 failures/12 passes) and are repaired:
admitted intent is permanently canceled on supersession; checked radio clicks can
start a new intent. Focused 14 tests now pass. Isolated sabotage removing the
supersession guard reopens its regression (1 failure/13 passes; positive copy14passes).
Eleven Chrome checks now pass, including retained-radio clicks, one-live-question
transitions, settled opacity, real focus and switching to reduced motion. Both
typechecks and final build pass; dismissal is aligned to the right edge.

Final independent recheck passed all3 separate probes against unchanged source
hashes (retained click, superseded intent, digit/focus); reference reread found no
additional concrete blocker. Candidate source custody and receipt are recorded in
`/tmp/harso-questionnaire-1478/`. This is scoped candidate proof, not Land acceptance.
The whole-library goal remains incomplete. WEV-1479 now supplies the consolidated
126-family requirement baseline at `/tmp/harso-library-batch-audit/requirements.json`;
do not restart whole-library discovery or equate exports with completion.

## WEV-1477 — shared numeric chart local candidate

Observed RED:11failures/24passes across all eight shared bar families and Funnel.
Primary-only normalized scaling now preserves true zero/fractions, signed bars,
finite extremes and invalid-value isolation. Funnel retains all supplied stages
with nonnegative validation and zero width without padding. Values and labels are
outside geometry; affected chart containers use the continuous canvas treatment.
An initial narrow browser audit caught an unfocusable horizontal scroller; explicit
tab focus and visible outline fixed it, with actual ArrowRight scrolling checked.
The preview's invalid ExampleState comparison was corrected after typecheck.

Current UI379tests, both typechecks, isolated build and26Chrome checks pass;
wide/light funnel and narrow/dark bars inspected. Original-source sabotage fails
12tests/23passes. Independent review exercised108actual-source vector/family cases
and3explicit proportion checks, with no blocking numeric-root finding. It checked
signed/extreme/invalid/duplicate/secondary cases and shared CSS minima. Subpixel
or IEEE-754-underflow ratios can still have no visible pixels; exact numeric labels
remain truthful rather than inventing a minimum mark. Evidence and candidate custody:
`/tmp/harso-shared-charts-1477/`. This is not complete family parity or Land acceptance.

## WEV-1475 — CodeBlock local candidate

Observed RED reproduces omitted code under a custom header and altered CRLF/blank
lines. The body now remains present with header-only composition, without duplicating
explicit nested Content. Line numbers use generated presentation so source text
remains exact. A separate RED proves missing token highlighting; pinned Shiki4.4.3
now supplies real dual-theme tokens rendered as inert React text. Unknown languages
fall back to text; asynchronous results are checked against current code/language.
Dependency installation disabled scripts and reported zero audit vulnerabilities.

Final focused14tests and UI368tests pass; both typechecks and isolated build pass.
Ten Chrome checks cover actual Light/Dark×Clean/Cozy, wide/narrow, Axe, exact CRLF
copy/clipboard denial, keyboard/focus/Tab/Escape, host refusal and disabled actions.
Wide/light and narrow/dark screenshots inspected. A controlled late tokenizer result
cannot overwrite new plaintext. Original-source sabotage fails12tests/2passes.

Independent review found extracted content duplication/nested-block suppression and
hidden-ancestor navigation. New RED tests reproduced both; nearest-context layout
registration and shared visible-choice filtering repair them. Reviewer rechecked in
Chrome, including composed Value and prevented actions, with no remaining concrete
browser blocker in targeted probes. Custom-header automatic body is browser-resolved;
server-only output requires explicit Content until hydration (documented, no SSR
consumer claimed). Initial browser test used the wrong catalogue prefix; corrected
to vercel:code-block before the successful run. Async-test harness corrections are
recorded separately from product findings.

Dependency review verified all16new Shiki lock entries against registry signatures,
tarball hashes and installed bytes. Provenance decoded, not Sigstore-verified; no
install lifecycle hooks. Existing/new large grammar chunk warnings remain. Evidence,
review and source custody: `/tmp/harso-code-block-1475/`. No desktop adoption, release,
Land acceptance or whole-library completion is claimed.

## WEV-1473 — Medical Profile local candidate

MedicalProfile now composes identity, steps, sleep breakdown, active-day calendar,
activity rings, alert feed, patient table and a local report draft through the
existing controlled workspace shell. All identities/readings are synthetic; no
clinical assessment, treatment instruction or real health service is involved.
Report drafts and alert-read state are profile-scoped, with controlled admission,
search/filter/sort/pagination/selection and refusal paths.

Original-source RED:1failure/9passes. UI suite356passed; both typechecks and
isolated build passed. Independent source/arithmetic review found no bounded
blocker and checked all seven profile scales, zero day and maximum day.
The first browser failure was an ambiguous substring locator matching both
the request output and hold-request checkbox; the exact-label correction passed.
Final12 Chrome checks pass after flattening alert surfaces, covering actual
expanded/compact layouts, Light/Dark×Clean/Cozy, Axe, calendar keyboard interaction,
profile isolation, zero geometry, filtering/admission changes, report refusal and
disabled-error retry. Wide/light and narrow/dark screenshots inspected. Both final
typechecks and build pass, with the existing nonblocking bundle-size warning.
Disposable original-source sabotage fails1test with9passing. Candidate custody
and evidence: `/tmp/harso-medical-profile-1473/`.

All nine template families now have original compositions, but this does not
prove whole-library completeness. Full shared-family anatomy/API/state/interaction
audit, known chart and syntax-highlighting gaps, and current public catalogue
reconciliation remain open. No desktop adoption or Land acceptance is claimed.

## WEV-1472 — Marketing Dashboard local candidate

The header-only mapping is replaced by an original responsive workspace with
acquisition funnel, spend half gauge, traffic tabs, spend/ROAS and visitors charts,
campaign filters/table and validated local campaign creation. All data is synthetic;
historical analytics and editable campaign rows have explicitly separate meaning.
No Pro source, ad account, network, persistence or runtime authority is introduced.

Observed original-source RED and disposable sabotage each fail one composition
test with seven passing. UI suite:354 passed; both typechecks passed. Independent
counterexample found document-global navigation could target another instance;
navigation now uses a root ref, with a preceding-decoy Chrome regression. Visual
inspection exposed the gallery's650px cap preventing expanded-layout coverage;
the scoped Marketing preview now allows full width and tests assert actual compact
state, not just viewport dimensions. Final12 Chrome checks pass across actual
expanded/compact layouts, Light/Dark × Clean/Cozy, Axe, interactions and error
states. Wide/light and narrow/dark screenshots were inspected. Both typechecks
and isolated build pass; the existing large-bundle warning remains nonblocking.
The refusal switch is inside the modal so the test never bypasses background
interaction isolation. Candidate custody/evidence: `/tmp/harso-marketing-dashboard-1472/`.

Medical Profile and the complete shared-family API/state/interaction audit remain
unfinished. This entry is not Land acceptance or desktop adoption.

## WEV-1471 — HR Management local candidate

The original people composition now includes the responsive workspace, KPIs,
recent hires, pipeline, fixed-scale engagement radar, twin-axis movement,
team tabs and employee table. Host requests support add/cancel/refusal/status,
filters, sorting, selection and pagination; all data is explicitly synthetic.
352 UI tests, both typechecks, isolated build and 12 Chrome tests pass, including
Light/Dark × Clean/Cozy, wide/narrow/Axe, zero/scaled geometry and disabled-error.

Independent review found boolean-false chart slots creating empty landmarks;
RED reproduced this and filtering absent content fixed it. Browser checks also
reproduced narrow implicit-grid overflow and disabled Tabs hiding selected
content. The HR grid now uses minmax(0,1fr); a native disabled fieldset preserves
the selected team panel without changing shared Tabs. Review rechecked the
deltas; original-source sabotage fails one test with five passing. Wide/narrow
screenshots inspected. Evidence: `/tmp/harso-hr-management-1471/`.

This is a local candidate, not Land or desktop adoption. Marketing Dashboard,
Medical Profile and the full shared-family parity audit remain unfinished.

## WEV-1470 — Home Dashboard local candidate

HomeDashboard now composes the existing workspace shell, navigation/panel,
breadcrumb/search, hires, metrics, earnings/revenue/contributions and customer
table slots. Its original host example has synthetic period-derived metrics,
customer search/filter/sort/selection/pagination/status controls, notifications
and local ticket creation. No backend/persistence authority is introduced.

Observed original-source RED and sabotage each produce one failure/three passes.
350 UI tests, both typechecks, isolated build and 12 Chrome tests passed on the
final source. Independent review identified a retry action that
ignored disabled state; a browser regression reproduced it and the corrected
suite passed. The final layout gives contributions a full row and routes narrow
navigation through the existing drawer rather than duplicating the sidebar.
The corrected retry was independently rechecked; wide/narrow visuals were
inspected, and hire navigation, ticket validation/cancel/refusal and keyboard
actions verified. Evidence: `/tmp/harso-home-dashboard-1470/`. This is a local
candidate, not Land acceptance or desktop adoption. Three other templates and
the complete family parity audit remain open.

## WEV-1469 — AI Profile local candidate

The profile composition now has browser evidence for edit/save/cancel, local
clipboard sharing, profile switching, month/day inspection and data states.
The first browser regression reproduced zero-agent days rendered as four-pixel
bars. The profile consumer now supplies zero-correct geometry through the
existing OrdersChartCard children slot; shared chart-family parity remains open.
The unit test checks disabled background actions before opening the modal rather
than incorrectly querying them through the modal's accessibility isolation.

Current checks: 349 UI tests, UI and preview typechecks and isolated preview build
pass. All 11 Chrome tests pass, including the theme/width matrix and
host-refusal/validation and keyboard-focus/action checks. Native select popup ArrowDown automation did
not commit a selection on this Chrome/macOS runner: the test proves focus and
selection via selectOption, not native popup keyboard selection.
Narrow labels now have measured readable width in a focusable horizontal scroll
region. Original-source sabotage produces two expected failures/eight passes.
Independent source review and final scroll delta review found no concrete bounded
blocker. Evidence: `/tmp/harso-ai-profile-1469/`. This is a local candidate, not
Land acceptance, full-library completion or desktop adoption. Four other template
compositions and the shared-family parity audit remain.

## Objective

Complete the original Harso UI kit in the founder-approved boundaryless
direction, with one-to-one coverage of the public BoardUI and Vercel AI Elements
catalogues. Do not redefine completion around the first useful subset.

Outcome: WEV-1422. Design ruling: WEV-1423, accepted design receipt
`sha256:24dc97a13868b6733040bba1e1c9acd92a13638bca4d993fa463163887e9b1d4`.
Source acceptance remains Land; no releases or main merges are performed here.

## Current checkpoint

### Image-generation composition — WEV-1468

Replaces the pending-image figure with an original AiChat-based workspace:
agent/history navigation, prompt thread, host state/estimated countdown, image
frame, CSS ripple/unfold/reveal, feedback/copy, configured composer/status and
masonry gallery in the existing single drawer. Synthetic examples capture model,
style, aspect and reference names per generation; drafts and pending generations
remain agent-scoped. No provider/upload/persistence or download implementation.

Initial RED and old-source sabotage reopen three checks while five prior checks
pass. Independent review exposed stale failed-media state overriding idle/stopped;
the failing regression now passes and six independent state/submission probes
rechecked the correction. Chrome found a native submit button inside the existing
div-based Composer; reusing PromptInputSubmit fixes the actual callback route.
Visual inspection found the generic thread's fixed height separating prompt and
output; a template-scoped natural-height thread fixes it, with a browser bound.

Evidence lives in `/tmp/harso-image-generation-1468/`; final receipt records the
candidate identity and verified counts. This composition is not full-library
completion or desktop integration. Five other template compositions and the
documented shared-family parity/state verification gaps remain.

### Chat Starter composition — WEV-1467

The former suggestions-only preview now composes the existing AiChat shell
with history, team/account menus, chat, dashboard and sign-in/sign-up pages.
All state remains in the synthetic example host; team switching resets that
example, export only displays Markdown, and authentication forms neither
perform authentication nor retain submitted credentials.

Initial RED and old-source sabotage each reopen two composition/disabled
failures while three existing agent-surface checks pass. Independent review
found that deletion auto-opened a fallback without clearing unread; a Chrome
counterexample reproduced it. The corrected transition marks the fallback read
only when its chat is displayed. Eight independent state probes rechecked
deletion across routes, background deletion, refusal, disabled and last-chat cases.

Browser inspection also found the history grid's intrinsic column could overlap
the conversation and block its action menu. A constrained grid track plus wrapped
button text contain titles and unread labels. A Range-based content-bounds check
guards label overflow, and explicit whitespace keeps readable accessible names.
Browser coverage includes chat/dashboard/signup themes, narrow sizes and Axe,
history actions, scoped drafts/pending replies, Stop/refusal and native forms.
Evidence is retained in `/tmp/harso-chat-starter-1467/`; the final native receipt
records verified counts and candidate identity. This is a template-composition
candidate, not Land acceptance, desktop integration or whole-library completion.
Six other template compositions and shared-family parity gaps remain.

### AI Chat workspace composition — WEV-1466

AI Chat is no longer a title/placeholder wrapper. Named slots compose navigation,
repository tree, recent conversations, message thread/feedback, composer,
optional header status and one Changes/Browser context panel. The catalogue
uses the library's real FileTree, Message, Composer, CodeBlock and sandboxed
WebPreview parts. Data, drafts, requests, local attachment names and manually
advanced synthetic replies are owned by the example host, never a new runtime.
Changing conversations closes the panel and preserves per-conversation drafts.

Initial RED and old-source sabotage each reopened two composition failures with
the existing agent-surface test passing. Independent review found focus-return
loss when resizing an open Radix panel; a real-browser counterexample failed
before logical-opener retention was added and passes afterward. The attachment
browser test also exposed delayed reading of a reset native FileList; the host
example now snapshots names during the selection event before scheduling state.
The panel is
nonmodal wide and modal compact, based on actual container width. Narrow and
wide screenshots were inspected. Unit regression has 342 passing tests;
typechecks and isolated build pass. Browser coverage includes host refusal,
send/stop/synthetic advance, feedback, attachment selection/removal, state
examples, conversation scoping, panel switching, theme/Axe/focus behavior and
responsive-mode transitions. Source acceptance remains Land.

This closes a template-composition gap, not full code-editor parity: shared
CodeBlock highlighting remains unfinished. Seven other template compositions
remain unfinished, alongside family-level behavior and verification gaps.

### Heatmap matrix — WEV-1465

Replaced opacity-only cells with a semantic two-axis table and native cell
inspection. Both inventoried layouts are demonstrated; labels, real values,
intensity ceiling/accent, host range refusal, missing/zero/invalid states and
disabled handling are implemented. Finance now supplies date/category data.
No observations is distinct from measured zero; partial totals name missing
cells. Pointer exit restores the keyboard-focused cell rather than leaving a
different hovered value active. Finance propagates disabled to its matrix.

Focused RED: five failures; old-source sabotage reopened five while seventeen
other chart checks passed. Review counterexamples produced three failures;
the corrected focused suite has twenty-four passing checks. Independent review
rechecked the corrections without remaining concrete blockers. Browser Axe
caught an empty table corner header, replaced with visible Series. Visual
inspection caught narrow labels wrapping into tall rows; minimum header widths
now preserve readable labels with internal scrolling, guarded by a row-height
browser assertion. Full UI regression has 340 passing tests; typechecks and
isolated build pass. Final Chrome evidence includes both theme palettes,
narrow/desktop, matrix interactions and Finance consumer checks. These are
local candidate proofs, not Land acceptance or completed whole-library parity.

### Weighted Sankey verification — WEV-1464

Sankey now renders supplied acyclic nodes/links with proportional flow widths,
source tint, separate source/sink totals, sink shares and inspectable node/link
values. Named or indexed endpoints, zero flow, invalid graphs, numeric overflow,
tiny positive weights, disabled controls and host-refused range changes have
focused checks. Catalogue and Finance use the real graph API.

Local verification: 334 UI tests, UI and preview typechecks and the isolated
preview build pass. Nineteen installed-Chrome checks cover Sankey and Finance,
including Light/Dark × Clean/Cozy at desktop and narrow sizes, scoped Axe and
actual ArrowRight scrolling of the narrow diagram. Restoring the old chart
source in a disposable copy fails five Sankey checks while eleven other checks
pass. Independent review reproduced two additional defects: spanning links
obstructing unrelated nodes and mixed-magnitude underflow hiding positive flow.
Both regression tests failed before correction and pass with intermediate
layout lanes and explicit relative-range rejection. Wide and narrow screenshots
were inspected. This is bounded local proof,
not Land acceptance, full-library completion or desktop adoption. The layout
preserves supplied order; crossing minimization is not implemented.

### Finance composition — WEV-1461

FinanceDashboard is no longer a stat-only Dashboard alias. It supplies named
navigation/actions, cash-flow, category-spending, portfolio, daily-spending and
transaction slots on a responsive continuous canvas. The catalogue uses real
DataTable search/category/sort/selection/pagination with host refusal and
loading/empty/error/disabled examples. All records are synthetic.

This is composition progress, not completed chart parity.
WEV-1465 now implements the heatmap's two-axis matrix, row/column focus,
cell-value inspection and range/intensity controls. Calendar dates are a
Finance composition concern, not a replacement for general matrix coverage
(weekday × hour and region × month are both demonstrated). This does not
finish the remaining Finance or chart-family audit.
WEV-1464 replaces Sankey strips with weighted DAG geometry, source/sink totals,
node/link inspection and host range control. WEV-1463 replaces the first-value
ring with independent goal arcs, readable values and a selected-day composition.
WEV-1462 replaces indexed scatter positions
with real series x/y coordinates, optional size encoding and inspectable values.
The remaining chart gaps require
their own chart work before the finance template can be considered complete.
Seven other template compositions remain unfinished; AI Chat now has a real
composition under WEV-1466 with its shared-code highlighting gap recorded. Public finance
documentation still names the seven constituents captured in the inventory.

Working-tree checkpoint — September 7, 2026: the catalogue currently exposes
125 reference families and 125 mapped family previews. The current source has
real behavior coverage for attachments, charts, prompt file selection, native
audio synchronization, image fallback, and sandboxed web preview. The full
library remains open: mapped preview is not equivalent to complete API/state,
responsive, visual, or cross-reference parity. WEV-1459 package evidence is
315 passing UI tests, passing UI/preview typechecks, and a passing isolated preview build; no Land, merge,
release, or desktop adoption claim is implied.

Historical WEV-1450 checkpoint (not the current library count):
WEV-1450 added17 Test Results parts after WEV-1449's10 StackTrace parts:
**208 UI exports /51 mapped family examples**. **74 families plus remaining
rich variants are still pending.** Mapping is not full API/behavior parity.
WEV-1446 corrects the captured inventory to125 families/422 UI-part references
and20 separate helper/type entries, not a compiled-export completeness claim. Desktop adoption
and Land acceptance remain separate; no brand artwork or old tokens imported.

The earlier four activity families expose all24 mapped UI parts, plus the
`getStatusBadge` helper and a local presentation-only `ToolPart` type.
Tool input JSON and Agent schemas deliberately remain plain code text:
shared syntax highlighting/CodeBlock composition still needs work. WEV-1460
repairs AgentTools single-selection ownership and controlled-empty behavior;
focused regression verification passes. Agent means configuration,
not an execution monitor. The library never approves or runs a tool.
Sources restrict supplied addresses to absolute HTTP(S) without credentials,
tracking pings or referrer transmission. Host-rendered React content remains
trusted composition; authorization/redaction happen before rendering.

## Authentication carousel repair — WEV-1457

WEV-1455's native diagnosis established 125 exported families and all 422
mapped UI-part references, but not behavior completion. Its three-probe report
and original source archive remain preserved in
`/tmp/harso-library-audit-CHOPMO/`; the diagnosis is closed, the library is not.

The carousel now normalizes controlled/local indices, reconciles child removal,
respects descendant editing and host cancellation, avoids boundary callbacks,
and follows RTL arrow direction. Indicators retain 7px artwork in 28px targets
with keyboard and forced-colors focus. No autoplay, device or runtime state.

Focused RED: 8 failed / 5 passed. Independent review found keyed-slide draft
loss and an overwritten native accessible name; both were reproduced (2 failed
/ 13 passed) and corrected. GREEN: all 15 carousel/misc tests pass.
A disposable descendant-guard removal reopens the editing-key test. Package
regression: 55 files / 278 tests; typecheck and isolated production build pass.
Nine isolated installed-Chrome cases exercise Light/Dark × Clean/Cozy at 1512px
and 390px, plus forced colors. Browser cases include editing/navigation, focus,
target dimensions, horizontal overflow and scoped axe checks; screenshots are
retained under `/tmp/harso-carousel-repair-DO0CLF/browser-results/`.
Default Playwright browser binaries were absent; installed Chrome was used
without downloading or installing anything. These checks do not accept the
whole library, finish the AuthCard family/template, or imply desktop adoption.

## Selector selection repair — WEV-1458

The shared Model/Mic/Voice selection path now supports explicit controlled-empty
`null`, host refusal, caller cancellation before state changes, root disabled and
read-only guards, and native pressed-button semantics. Duplicate item handlers
are replaced by one shared item implementation. Focus, selected weight/edge and
forced-colors cues are scoped to selector items, not global `aria-selected`.

Initial RED: 19 failing new checks and 2 existing positive controls. Initial
GREEN: 21 selector tests; full UI suite 55 files / 297 tests and typecheck pass.
Disposable cancellation-guard removal reopens all three sibling regressions.
Six installed-Chrome checks cover each sibling in Light/Clean desktop and
Dark/Cozy narrow layouts, scoped axe, focus and target dimensions; forced-colors
selected-edge check also passes. These are targeted checks, not the full theme
matrix. Proof lives at `/tmp/harso-selector-selection-1458/`.

At this historical checkpoint, the catalogue examples were shallow and
opening/search/dismissal and device-helper ownership remained follow-ups. The
WEV-1459 work below addresses those; this receipt itself did not prove them.

## Selector lifecycle and device presentation — WEV-1459

Replaces inert trigger/content/input/group/empty parts with original searchable
Model and Voice dialogs plus a trigger-aligned nonmodal Mic panel. Reuses the
installed Radix lifecycle; no new focus trap, dependency or vendor styling.
Shared filtering includes visible text/keywords and preserves host order.
Root-controlled open/search, native keyboard traversal, host cancellation,
closing/focus return and theme-aware portals accompany richer catalogue examples.

Automatic hardware enumeration and inferred permission are removed. Mic now
renders host device/permission/loading/error snapshots and exposes an explicit
optional request action, including async failure handling. No hardware API is
called by the kit. Voice preview represents host-supplied playing/loading state
as a sibling action; metadata/logo parts remain host-content composition.

Public reference input: `/tmp/harso-ai-elements-docs/{model-selector,mic-selector,
voice-selector}.mdx`, pinned by the catalogue to Vercel commit
`6a9d5b1822ffb10bba4bd97175f01edd7d8651cd`. Native button/search APIs and
host-owned device handling are explicit original-design differences, not hidden
drop-in compatibility claims. Full-library/template completion remains open.
Proof and remaining limitations are recorded at `/tmp/harso-selectors-1459/`.

Verification: 39 selector tests and the fresh full 55-file / 315-test UI suite
pass, as do both typechecks and the isolated Vite build (existing large-chunk
warning retained). The final 29-case installed-Chrome run covers all three
families in Light/Dark × Clean/Cozy at 1512px/390px, plus empty/read-only/long/
disabled, real outside-target dismissal, microphone state/width and voice
preview/forced colors. Screenshots retained; light narrow voice and dark cozy
microphone were visually inspected. Static axe waits for the 140ms entry
animation to finish rather than measuring an intermediate translucent frame.
Read-only tests use keyboard activation, not a click on an ARIA-disabled target;
outside clicks target a settled visible element instead of racing Radix's
deferred listener initialization with an immediate coordinate event.

Independent review found an enabled request action after root disable and
host-hidden groups counted/navigated. Both are corrected and rechecked against
exact current source: 39 permanent plus the 3 unchanged independent checks pass,
with separate Chrome evidence. Group `hidden` and inline visibility props are
supported; arbitrary external CSS/media visibility tracking is not claimed.
Disposable fuzzy-filter sabotage reopens all three lifecycle regressions.
This is a verified selector construction candidate, not Land or full-library
completion; desktop, backend, device services and releases remain untouched.

## Historical foundation checkpoint — WEV-1425

WEV-1425 builds the full reference map, initial native foundations and an isolated
catalogue. **The full library is not complete.**

- Initial capture: 125 reference families; 395 named subcomponent references.
  WEV-1446 later corrects omitted anatomy and classification; this historical
  checkpoint does not establish complete documented anatomy.
- Original native exports: Button, IconButton, Link, Input, Textarea, Field,
  Checkbox, Switch, RadioGroup, Select, Slider, Progress, Badge, Avatar, Skeleton,
  Separator, EmptyState and Disclosure.
- Twelve reference families initially resolve to a native component.
  This is not a count of fully verified reference families.
- Controlled System/Light/Dark and Clean/Cozy foundation, no preference store.
- Searchable catalogue, source filtering, explicit pending states, direct
  component links, narrow navigation and interactive synthetic examples.
  The separate foundation picker exposes all 18 native exports individually.
- Existing desktop GUI and its earlier candidates are not replaced.

The foundation correction passes 112 kit tests, typed native-ref consumption,
the preview build and eight browser checks, including five existing snapshots.
The independent challenger rechecked its four findings: skip-link draft loss,
duplicate Slider labels, omitted native ref types and renamed-part search.
All four clear; the frozen candidate and evidence identity are recorded on
WEV-1425. This is local construction proof, not Land or desktop acceptance.
The catalogue deliberately exposes incomplete mappings.

## Complete remaining execution

### Navigation checkpoint — WEV-1427

Ten additional reference families now have original interactive examples:
Breadcrumb, Button Group, Chip, Close Button, Pagination, Segmented Control,
Tabs, Theme Toggle, Tooltip and Announcement. Their nine named parts are exported
and used by those compositions. This brings the library to 37 UI exports
(18 foundations plus 19 navigation/part exports), and 22 mapped family examples.
It does not make the other 103 mapped families implemented or close the rich
foundation variants. Reference differences are explicit in README and in the
catalogue's behavior notes. WEV-1427 construction proof: 121 kit tests, 16 browser
checks (eight foundation and eight navigation), typechecks/build and 11 visual
baselines; keyboard, touch, forced colors, appearance and clipped-row hints.
The independent direct-TabList keyboard-entry finding is corrected and rechecked.
Native controlled-form reset remains an explicit host responsibility, not a
second library state store. Exact candidate/evidence receipt belongs to WEV-1427;
none of this is Land acceptance or a complete library claim.

### Native entry and choice checkpoint — WEV-1429

Ten more UI exports add OTP, metadata-only file selection, telephone input,
choice-card/radio parts, native SelectItem, Kbd and a controlled range pair.
Seven existing family pages retain their basic examples and gain separate
interactive compositions; Input OTP and File Upload add two new family pages.
Cumulative coverage is **47 UI exports / 24 mapped family examples**. The other
101 mapped families are not implemented; native rich variants and intentional
interaction differences remain explicitly tracked in README.

The September 6 Brand Identity logo finishing sheet was inspected and recorded
as influence. Older branding and its ongoing typography study do not override
the approved boundaryless direction. No logo was copied into the desktop.

This checkpoint remains local construction, not Land acceptance, a release,
completed family-by-family parity, or desktop adoption. The WEV-1429 terminal
receipt must identify its final candidate and remaining limitations.
Corrected construction checks: 133 kit tests, both typechecks and preview build
pass; 24 browser checks cover native interaction, all palettes, RTL, touch,
forced colors and 17 unchanged visual baselines. Independent challenge found
input remount, inherited-disabled drop and range/native numeric disagreement;
the consolidated correction preserves input identity, consults native disabled
state and removes custom range snapping. Native values drive output/FormData;
browser precision failure disables selection. RED/GREEN and restored sabotage
are separately recorded. WEV-1429 is now FROZEN with receipt
`sha256:1afc91335daaa23055eb2c562cffc24830afc3cc15cf1fa2efaaa6c12ab6ec06`;
the exact corrected-source challenger/verifier passed. This is not Land.

### Composable navigation surfaces — WEV-1431

Dropdown's six named parts, Sidebar/SidebarItem and Carousel/CarouselItem add
ten original exports and three meaningful catalogue family examples. Cumulative
coverage is **57 UI exports / 27 mapped family examples**, not a complete kit:
**98 families remain**, together with recorded rich-variant gaps.

Menus exercise native top-layer positioning, viewport collision/fallback,
controlled refusal, grouped choices/actions, nested keyboard navigation,
trigger focus restoration and native editable-content ownership. Sidebar
examples cover dashboard and AI repository navigation, search, account/workspace
menus, announcements and retained drafts across collapse. Carousel examples
cover full/peek/mixed/compact widths, start/center alignment, optional controls,
real boundaries, native scrolling, resize, RTL, keyboard and emulated touch.

Independent counterexamples exposed editable menu keys stealing focus and
carousel backward navigation becoming stuck when several small cards shared a
clamped endpoint. Both were corrected and independently rechecked. Native focus
return, scroll-region accessibility and whole-chain nested action dismissal also
have targeted browser regressions. The candidate uses existing primitives,
native popovers, CSS anchors and scroll snap; no new dependency or data authority.

Four historical full-page snapshots encoded the old implementation count. Their
bounded replacement belongs to WEV-1433, not an undeclared WEV-1431 edit:
`sha256:90e05084d98231c10439ce04849daa8d274f705379547b49685335e1c025d853`.
Original PNGs remain in the prior freeze. WEV-1433 discloses three extra cozy-dark
glyph-edge pixels; no source/test/threshold changed in that leaf. Construction
proof and exact candidate closure belong to WEV-1431's immutable receipt; neither
leaf is Land or desktop acceptance.

### Remaining groups

Each group is implemented through bounded leaves. Shared files have one writer;
parallel support is limited to independent discovery or verification.

1. **Reference-complete native and selection controls.** Audit the remaining
   sizes/states, rich select icons/avatars/search, phone masks, date input,
   field states and social buttons. WEV-1429 adds indeterminate/cards, native
   select descriptions, input adornments, OTP and metadata-only file input;
   documented native substitutions do not imply rich donor equivalence.
2. **Remaining navigation, disclosure and attention.** Notifications/center,
   auth/settings compositions and empty/error
   states. Preserve accessible focus, escape, modal and nonmodal distinctions.
   WEV-1427's ten families and WEV-1431's three families are covered above,
   not still-unbuilt placeholders.
3. **Conversation and composer.** Message/branch/action parts, attachments,
   sources/citations, suggestions, context, model selection, expandable composer,
   speech affordances, loading and streaming states. Build the approved continuous
   canvas composition as a real showcase, not a background screenshot.
4. **Work and AI activity.** Agent, task, queue, plan, reasoning/activity summary,
   checkpoint, approval/confirmation, question, tool, agent progress/thinking,
   limits, task list and web search. Show only supplied observable progress, with
   clear state transitions, unavailable/stale cases and host callbacks.
5. **Developer content and results.** Artifact, code/snippet, commit, file tree,
   environment variables, package/schema display, terminal, stack trace, test
   results, web/JSX preview and sandbox. Add explicit security/privacy leaves
   where content or execution boundaries require them. No arbitrary eval.
6. **Media and voice.** Image, audio player/control parts, microphone/voice
   selection, persona, speech input and transcription. Host supplies media,
   capabilities and lifecycle; no permissions on mount or simulated live audio.
7. **Data and charts.** Table/data-table and all 19 documented BoardUI chart
   families. Meaningful datasets, selection/hover/keyboard/table equivalents,
   empty/partial/error states and truthful units. Decorative SVG alone is not
   complete chart behavior.
8. **Workflow canvas.** Canvas, node, edge, connection, panel, toolbar and
   controls. Real local pan/zoom/selection and accessible navigation, with
   externally controlled graph data and no second workflow engine.
9. **Templates and foundations.** Color/type reference components, calendar and
   all nine BoardUI template families, expressed as original reusable Harso
   compositions. Use clearly labelled sample data and supplied actions, not
   fabricated product capabilities or copied donor screens.
10. **All-library audit.** Every reference and named part has a concrete export,
    use example, behavior/state disposition and verification evidence. Check
    imports, tree-shaking, keyboard/touch/focus, contrast/zoom/forced colors,
    reduced motion, Light/Dark/Clean/Cozy, responsiveness, long content and
    composition. No generic fallback example counts as a finished component.

## WEV-1434 — date and calendar construction

DatePicker maps to `DatePicker`, `DateRangePicker`, `MonthPanel` and
`MeetingScheduler`. Calendar maps to `Calendar` with month, event details,
attendees/action callbacks, inbox and event-overflow compositions. Five new
exports bring the working checkpoint to62 UI exports and29 mapped family
examples. **96 families, rich-variant gaps, full-library composition and desktop
adoption remain unfinished.** This is construction progress, not Land acceptance.

Reuse: native date fields, automatic popovers, `Intl`, UTC Gregorian arithmetic,
existing Button/Input/Field/Select/Disclosure and Dropdown. The embeddable month
grid is original because native date fields do not expose an inline calendar.
There is no imported Pro source, added dependency, availability provider,
booking engine, external-calendar sync or new persistence/runtime authority.
Brand influence remains separate; no logo or old brand-book tokens are imported.

Proof and freeze are recorded by the WEV-1434 receipt, not inferred from this
working note. Prior58-file archive remains unchanged. Four prior foundation
images are retained before a count-only29-family refresh; new date/calendar
snapshots exercise native top-layer layout and responsive composition.

## WEV-1437 — conversation construction

Conversation, Message, Suggestion and Shimmer add20 original UI exports and
four mapped family examples. `messagesToMarkdown` is separately accounted for
as a pure formatting helper. Working checkpoint: **82 UI exports /33 mapped
family examples**; **92 families and rich-variant gaps remain**. Public named
parts are implemented, but Message math/highlighting/attachments are explicitly
pending later result/media composition, not claimed as complete family parity.

Native scrolling/ResizeObserver, shared Button/IconButton/Tooltip/EmptyState,
and installed react-markdown/GFM are reused. The library adds no transport,
retry identity, session store, token engine, persistence or SDK adapter. Host
callbacks own download and actions. The synthetic catalogue supplies observable
text updates, branch refusal/drafts, empty and streaming states, safe Markdown
negatives, suggestions and static/moving text. Browser proof caught focus loss
on a disappearing jump button and narrow suggestion overflow caused by the
base button's later nowrap rule; local corrections preserve focus and override
that rule without changing shared primitives or masks.

Current construction and pending assurance evidence belong to WEV-1437. Its
terminal receipt, not this note, will identify the final candidate and results.
The predecessor WEV-1434 is FROZEN with receipt
`sha256:121bbd221ebd8cfabf5875e1a05e67efdb7527131d669ca93cbcf09d3b937e95`;
its archive and proof remain unchanged. FROZEN is not Land acceptance, desktop
adoption, or full-library completion. Brand influence is unchanged.

## WEV-1439 — compact work construction

Plan, Task, Reasoning and Checkpoint add19 original named UI parts; the mapped
`useReasoning` hook is counted separately. Working checkpoint: **101 UI exports
/37 mapped family examples**; **88 families and rich-variant gaps remain**.
One private native-button disclosure serves three real consumers. Existing
Shimmer, Button, Tooltip and sanitized MessageResponse are reused without new
dependencies, timers, stores, runtime authority or file/network operations.
The prior WEV-1437 construction is FROZEN under receipt
`sha256:7f07df1d5c39a45688ec04ca187d64b7b8f1e508ff94320f4e05414a4113ea3d`.

Work stays compact until explicitly expanded; collapsed details retain drafts,
external collapse restores only displaced focus, nested work stays independent,
and host progress updates never reset disclosure. Public summary duration is
host supplied. Required decisions sit outside collapsed content. Checkpoints
and file actions are requests only. Named upstream automatic-open/timing/API
differences are recorded in README and catalogue notes rather than hidden.
Security/privacy proof is limited to the supplied summary-text boundary; no
real backend, user history or model-chain exposure is introduced. The source
and evidence freeze belong to WEV-1439, not this in-progress checkpoint.

## WEV-1441 — tables and metrics construction

Table, DataTable and StatCards add three UI exports and mapped family examples.
Working checkpoint: **104 UI exports /40 mapped family examples**; **85 families
and rich-variant gaps remain**. The then-captured125-family/395-part reference
inventory was unchanged by this leaf; WEV-1446 later corrects it. Native table/caption/headers and definition-list semantics reuse
Checkbox, Button, Pagination, Tooltip and host-rendered cells. No table engine,
dependency, persistence, transport or backend authority is added.

Rows, sorting, filtering, selection and pages remain controlled by the host.
Selection callbacks emit eligible visible ID deltas only. Invalid identities
fail closed; loading/error hides stale row actions. Custom React slot actions
remain the host's responsibility, not implicitly protected by table props.
The narrow visual check caught single-letter status wrapping; native word
widths now produce local horizontal scrolling instead of compressing columns.

The predecessor WEV-1440 is FROZEN under receipt
`sha256:821b74ab16f67778ff49c145f25e17e07f672ce5c2d54b334f187e5e87be7c91`.
Its source/evidence remain unchanged. WEV-1441's terminal receipt owns this
candidate's exact identity and proof, not this working note. Desktop adoption
and Land acceptance are separate. Brand influence remains unchanged.

## Full-library completion audit rules

The consolidated WEV-1479 baseline is the current gap authority: 126 families,
422 captured part references, 21 helper entries and 2,841 individually addressable
requirement rows (some descriptions overlap). Export presence does not establish
behavioral coverage. Keep this baseline pinned rather than repeating discovery.

WEV-1486 replaces Persona's unstyled variant suffixes with six original native
visuals and five host-driven states. Its DOM/CSS renderer substitutes explicitly
for Rive/WebGL; remote-loading callbacks are not fabricated. Renderer readiness,
motion start/pause/resume/stop and live reduced-motion behavior receive separate
tests. The reference mapping records this native-renderer difference rather than
claiming the vendor's assets or lifecycle payload compatibility.

The WEV-1483 Settings candidate adds the four-page controlled modal lifecycle,
body-portal theme inheritance and host-content slots. The shared gallery now
routes TaskList and WebSearch to the WEV-1482 companion examples instead of the
former shallow samples. These are bounded candidates, not completion of all
agent/shell families. Persona's native-renderer replacement is recorded above;
the detailed chart modes are tracked by WEV-1485. The wider library still requires
requirement-level proof.

For every entry in `catalog.json`, verify implementation against the documented
purpose, parts and variants and the original Harso design. A shared internal
primitive is encouraged, but every mapped component must have a meaningful
public API and an inspectable example. Record deliberate behavioral differences;
do not silently omit an awkward requirement. Missing, untested or only
placeholder-covered entries keep WEV-1422 and the thread goal active.

Classify references accurately: nine full template families are part of this
outcome; five duplicate showcase routes are not additional families; SDK
transport APIs remain explicitly outside the visual-library objective. Public
catalogues are pinned so later upstream additions are identifiable separately.

## Historical custody (earlier leaves)

Current worktree: `codex/worktrees/5605/weave-cloud`, existing branch
`codex/desktop-conversation`, HEAD `f973ec12abe7e2a222e3b88fdedf750dcadc0fa3`.
Do not commit or create branches without authorization. Preserve prior dirty
state, `.codex/`, other session work and the 4186 desktop preview. Dedicated
library dev preview: 4191; disposable browser test preview: 4192. All generated
proof and receipts use `/tmp/harso-1425-*` for the first source leaf.
# WEV-1445 — developer content construction

Snippet, PackageInfo and EnvironmentVariables add25 named UI exports and three
real mapped family examples. Working checkpoint: **153 UI exports /47 mapped
family examples**; **78 families and rich-variant gaps remain** against the
then-captured125-family/395-part inventory, later corrected by WEV-1446. No complete-library, desktop
adoption or Land acceptance claim.

Reuse: native Clipboard.writeText on explicit request, existing Input,
IconButton, Switch, Badge and theme tokens, plus React context for composition.
An internal copy control serves two actual consumers; no SDK, highlighter,
permission probe, shell executor, package installer or dependency was added.
Snippet code is inert/selectable. Package versions and change labels are supplied.
Environment values mask custom children too; value/export copy requires reveal,
and POSIX export validates names and quotes values without interpolation.
Copy completions are tied to current content and visibility. Host-supplied
React composition and identity/remount are outside the masking boundary.

The new batch preserves all earlier frozen archives. Eight existing catalogue
PNGs may update only for count/availability metadata with originals retained;
new developer snapshots show the actual components. Construction evidence and
remaining assurance status belong to WEV-1445. The latest Brand Identity is an
influence, not a reset: no logo or older palette/type tokens are imported.
