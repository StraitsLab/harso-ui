var page=H.page('Controls');var changes=[];sketch.find('SymbolMaster',page).forEach(function(s){var bits=s.name.split('/');var app=bits[1]+'/'+bits[2];var st=bits[bits.length-1];if(bits[0]==='Switch'){var track=s.layers.find(l=>l.name==='track');var thumb=track.layers[0];thumb.style.fills=[{color:H.sw(app,st==='on'?'inverse':st==='disabled'?'tertiary':'ink'),fillType:sketch.Style.FillType.Color}];changes.push(s.name);}if(bits[0]==='Button'&&bits[3]==='primary'&&st==='hover'){s.style.fills=[{color:H.alpha(H.hex(app,'ink'),0.86),fillType:sketch.Style.FillType.Color}];changes.push(s.name);}if(bits[0]==='Input'){if(st==='invalid')sketch.find('Text',s).filter(l=>l.text==='A new beginning').forEach(l=>l.text='Project name');if(bits[3]==='search')sketch.find('Text',s).filter(l=>l.text==='⌘K').forEach(l=>l.style.textColor=H.sw(app,'secondary'));}if(bits[0]==='DatePicker'){sketch.find('Text',s).filter(l=>l.text==='September 2026').forEach(l=>l.frame.width=172);}});H.out({changed:changes.length});