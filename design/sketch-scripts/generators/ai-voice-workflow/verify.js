var p=H.page('AI Voice & Workflow');function all(l){var a=[];(l.layers||[]).forEach(function(k){a.push(k);a=a.concat(all(k));});return a;}var list=all(p),symbols=list.filter(function(l){return l.type==='SymbolMaster';});H.out({page:p.id,sheets:p.layers.length,symbols:symbols.length,uniqueNames:Array.from(new Set(symbols.map(function(l){return l.name;}))).length,micLabels:list.filter(function(l){return l.type==='Text'&&l.text==='Input level';}).map(function(l){return {color:String(l.style.textColor)};}),canvasEdges:list.filter(function(l){return /^port-bound edge/.test(l.name);}).length});