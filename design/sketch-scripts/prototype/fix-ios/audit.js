function all(l){var a=[l];(l.layers||[]).forEach(c=>a=a.concat(all(c)));return a;}['Proto iOS Light','Proto iOS Dark'].forEach(n=>H.page(n).layers.filter(l=>l.name.indexOf('Screen/ios/')===0).forEach(s=>{var a=all(s);H.out({page:n,id:s.name.split('/').pop(),frame:s.id,x:s.frame.x,y:s.frame.y,w:s.frame.width,h:s.frame.height,indicator:a.filter(l=>l.name==='home-indicator-native').map(l=>l.frame),status:a.filter(l=>['cellular-signal','wifi','battery'].indexOf(l.name)>=0).map(l=>l.name),links:Array.from(new Set(a.filter(l=>l.name.indexOf('link:')===0).map(l=>l.name))),badCopy:a.filter(l=>l.type==='Text'&&(/iOS prototype|Every 1d|●●●|account service/.test(l.text))).map(l=>l.text),scrim:a.filter(l=>l.name.indexOf('overlay-scrim')===0).map(l=>String(l.style.fills[0].color))});}));