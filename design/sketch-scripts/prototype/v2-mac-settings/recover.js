var names=['Screen/macos/Light/Clean/settings','Screen/macos/Light/Clean/settings-account'];var out=[];doc.pages.slice().forEach(function(p){p.layers.slice().forEach(function(s){if(names.indexOf(s.name)>=0){var target=H.page('Proto macOS Light');if(p.id!==target.id){s.parent=target;out.push({id:s.id,name:s.name,from:p.id});if(p.layers.length===0)p.remove();}}});});console.log(JSON.stringify(out));