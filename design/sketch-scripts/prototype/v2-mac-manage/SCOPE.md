## THIS LANE: `mac-manage` — macOS only, both appearances (Light/Clean, Dark/Clean)

Flow group 1 (projects + library): `projects` (grid of project cards: Personal (selected), Weave Cloud, Aheli ops; create row with name input + Create; card → `link:project`), `projects--empty`, `project` (project header 'Personal' + description, tabs Conversations/Work, 'Recent conversations' list (row → `link:conversation`), 'Work' list (row → `link:activity--work`), inline composer 'Start a conversation in Personal'), `recent` (grouped Today/Yesterday conversation rows with project chip + time; row → `link:conversation`; header 'New conversation' → `link:new-conversation`), `artifacts` (grid of artifact cards: markdown doc, code diff, image; card → `link:artifacts--detail`), `artifacts--empty` ('No artifacts are published yet' EmptyState), `artifacts--detail` (artifact panel from kit Artifact family: tabs Preview/Code, actions; close → link:back), `routines` (table of routines: Daily brief every 1d active, Weekly digest paused; Run/Pause/Edit actions; 'New routine' → `link:routines--new`), `routines--new` (Dialog with name, prompt textarea, schedule select, profile select; Cancel → link:back), `customize` (two sections: Profiles (cards General (selected)/Reviewer/Research with readiness chips) and Skills (list with toggles); card → `link:customize--profile`), `customize--profile` (profile detail: name, description textarea, model selector, tools list with checkboxes, Save).

Pages: `Proto macOS Light` / `Proto macOS Dark`. Use flow-group row y as given by your group number; x by screen index. Write one generator per screen parameterised by `var APP` and run it for both appearances. Keep scripts in `/tmp/harso-sk/proto/lanes/mac-manage/`. Never touch other lanes' screens or the kit pages. Never save.
